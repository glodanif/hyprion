name: hyprion
description: "Display profiles manager"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1
  toml: ^0.18.0
  path_provider: ^2.1.5
  go_router: ^17.1.0
  fpdart: ^1.2.0
  flutter_svg: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon_monitor_transformation.svg
